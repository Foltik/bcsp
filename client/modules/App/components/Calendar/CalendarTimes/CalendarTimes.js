import React, { Component } from 'react';
import PropTypes from 'prop-types';

class CalendarTimes extends Component {
  render() {
    return (
    );
  }
}

CalendarTimes.propTypes = propTypes = {
};

export default CalendarTimes;
